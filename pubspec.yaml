name: chatbot_tfg
description: "A new Flutter project."

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  googleapis: ^11.0.0
  googleapis_auth: ^1.5.0
  google_sign_in: ^5.0.2
  uuid: ^3.0.4
  flutter_secure_storage: ^9.2.2
  url_launcher: ^6.0.9
  flutter_datetime_picker: ^1.5.1
  extension_google_sign_in_as_googleapis_auth: ^2.0.12

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/seuArquivoJsonAqui.json # Coloque seu arquivo JSON aqui.
